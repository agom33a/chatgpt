# مشروع أتمتة اشتراكات ChatGPT — التسليم الكامل (v2)

> **اقرأ ده كامل قبل أي تعديل.** كل قرار هنا مبني على رد API **مسجّل فعلياً**.
> أغلب "التحسينات البديهية" اللي جُرِّبت كانت غلط لأسباب موثّقة تحت. لو لقيت
> نفسك بتخمّن، ارجع للتسجيل — ده الدرس الأول والأهم من المشروع كله.

> **للمساعد في المحادثة الجديدة:** إنت كملت مشروع طويل مع أحمد. مش محتاج
> تعيد شرح أي حاجة له — هو عارف مشروعه. اقرأ ده، افهم الوضع، وكمّل من "اللي
> فاضل" مباشرة. الأسلوب: تعديلات دقيقة بأوامر `python - << 'PYEOF'` على
> السيرفر، مع التحقق بعد كل تعديل. عربي مصري، مباشر، بدون حشو.

---

## 1. الهدف والاقتصاد

أحمد يبيع اشتراكات ChatGPT Plus لعملاء الشرق الأوسط. الأتمتة بتعمل اشتراك
كامل على حساب العميل بأرخص سعر، وتحمي فلوس أحمد من التجديد.

- **السعر الفلبيني** `₱982.14` (≈ $17) بدل الأمريكي ~$20
- **عنوان أمريكي** (Arkansas) → ضريبة صفر بدل 10–14%
- **بعد الدفع:** إلغاء التجديد + حذف كارت أحمد من حساب العميل

**ليه حذف الكارت؟** الكارت بتاعت أحمد بتتخزّن على حساب العميل بعد الدفع.
أحمد بيشحنها ويعيد استخدامها لعملاء تانيين — فأي تجديد على حساب قديم بيخصم
من أحمد. فحذفها حماية لفلوسه، مش قرار بالنيابة عن العميل.

---

## 2. الوضع الحالي — كل الطبقات شغّالة ✅

آخر مسار كامل ناجح (`auto_pay.js` على السيرفر):

```
1. فحص الخطة       → 404 = حساب free = كمّل  |  200+نشط = قف
2. رابط فلبيني      → عبر impulse
3. ملء + ضريبة صفر  → عنوان أمريكي
4. دفع              → confirm API يؤكد النجاح
5. إلغاء التجديد    → 3 طبقات مقاسة (page/file token, then reload)
6. حذف كارت أحمد    → مع تبديل الافتراضية لو لزم
```

**الوقت:** ~230 ثانية · **الضريبة الموفَّرة:** ₱117–137 شهرياً

---

## 3. البنية التحتية

### السيرفر (النسخة الموثوقة للكود هنا)

```
IP        : 72.60.184.150
المستخدم  : root
المسار    : ~/chatgpt          (لازم cd ~/chatgpt قبل أي أمر)
النظام    : Ubuntu, Node v20, CloudPanel
Chrome    : /usr/bin/google-chrome   (لازم Xvfb، ممنوع --headless)
```

⚠️ **مهم:** كل التعديلات الأخيرة اتطبقت على السيرفر مباشرة. النسخ المحلية
عند أحمد وفي outputs **متأخرة**. النسخة الموثوقة الوحيدة على السيرفر. أول
خطوة في المحادثة الجديدة: أحمد ينزّلها بـ
`scp root@72.60.184.150:~/chatgpt/auto_pay.js .`

سيرفر تاني (بوت CapCut، منفصل): `82.29.168.93`.

### أمر التشغيل

```bash
cd ~/chatgpt
xvfb-run -a --server-args="-screen 0 1400x900x24" \
  node auto_pay.js --link-proxy=impulse --proxy=cheap --country=PH --watch=5
```

### البروكسيات (`proxies.json`)

```json
[
  { "name": "impulse",
    "proxyString": "http://042391b658dfc7c6787a__cr.ph:c47d30b134a59488@gw.dataimpulse.com:823" },
  { "name": "cheap",
    "proxyString": "http://4vnt9kwFZKflTW2:5tmvJIp3n9LVJyA@thehub.proxy-cheap.com:8080" }
]
```

| المزود | الاستهداف | Stripe | الاستخدام |
|--------|-----------|--------|-----------|
| **impulse** (DataImpulse) | `__cr.ph` في اليوزر، منفذ 823 | ❌ 403 | جلب الرابط فقط |
| **cheap** (Proxy-Cheap) | `_country-PH` في الباسورد، منفذ 8080 | ✅ | الدفع |

**قواعد مطلقة:**
- ممنوع `_session-` لـ Proxy-Cheap → `bad gateway`. الدولة وحدها.
- Stripe يخرج **مباشرة** (خارج البروكسي)، الوسيط بيرفضه بـ 500.
- `ip-api.com` يخرج مباشرة دايماً.

### باقي الملفات على السيرفر

```
session_api.json   جلسة الحساب — من https://chatgpt.com/api/auth/session
cards.json         [{"name","number","expiry","cvc"}]
billing.json       عنوان Little Rock, AR 72211, US (ضريبة صفر)
```

---

## 4. الـ APIs — كلها مسجّلة ومؤكدة بالقياس

```
GET    /backend-api/me
       → { country }   ويكشف السيشن الميتة بـ 401

GET    /backend-api/subscriptions?account_id=<id>
       → { plan_type, active_start, active_until, billing_period,
           will_renew, cancellation_outcome, billing_currency,
           is_delinquent, is_processor_stripe }
       404 "No subscription found" = حساب free  ← إجابة مش فشل
       خطة بـ active_until=null و is_processor_stripe=false =
         بقايا اشتراك متجر تطبيقات، الحساب free عملياً

POST   /backend-api/subscriptions/cancel
       body: {"account_id":"<id>"}   ← إجباري (بدونه 422 Field required)
       → {"returned_amount":null}
       بعدها: will_renew=false, cancellation_outcome="deactivate"

POST   /backend-api/subscriptions/renew
       body: {"account_id":"<id>"}   ← بيعيد تفعيل التجديد (للاختبار)
       → {}

GET    /backend-api/payments/payment_methods?account_id=<id>
       → { payment_methods:[{id,card:{brand,last4,exp_month,exp_year}}],
           default_payment_method_id, backup_payment_method_enabled }

DELETE /backend-api/payments/payment_method/<pm_id>?account_id=<id>
       → {"success":true}
       account_id لازم في الـ query (في الـ body → 422 loc:["query"])
       الافتراضية مبتتحذفش → 400 "Cannot delete default payment method"

POST   /backend-api/payments/payment_method/default
       body: {"payment_method_id":"<id>","account_id":"<id>"}
       → {"success":true}   ← لتبديل الافتراضية قبل حذف كارت أحمد

POST   /backend-api/payments/checkout/taxes
       → { checkout_session:{ currency, amount_subtotal, amount_total,
                              total_details:{amount_tax,amount_discount} } }
       بالوحدات الصغرى: 98214 = ₱982.14

POST   /backend-api/payments/checkout/confirm
       → {"status":"success","type":"payment_intent"}  ← دليل الدفع القاطع

POST   /backend-api/payments/checkout
       body فيه promo_campaign — بيتشال في الاعتراض عشان السعر الكامل
```

---

## 5. نتائج قياس الإلغاء (مصفوفة 11 طريقة، من cancel_lab3)

اختُبرت كل طريقة على حساب حي، مع إعادة تفعيل التجديد بينها:

| الطريقة | النتيجة |
|---------|---------|
| `{account_id}` + توكن الصفحة | ✅ 200 |
| `{account_id}` + توكن الملف | ✅ 200 |
| `{account_id}` + `?refresh` وحده | ❌ 401 (بيرجّع فاضي أحياناً) |
| `{account_id}` بدون توكن | ❌ 401 |
| `{}` / بدون جسم / في الرابط | ❌ 422 Field required |

**القواعد الثابتة:**
- الجسم `{account_id}` **إجباري**
- توكن **إجباري** (الكوكي وحدها مش كفاية)
- توكن الصفحة = `?refresh=true` **ثم** العادي (refresh وحده بيفشل)
- توكن الملف = احتياطي قوي **مستقل عن الصفحة**

**الطبقات في `stopAutoRenewal` حالياً:**
```
0. استنى الاشتراك يظهر (404 لثواني بعد الدفع طبيعي)
1. {account_id} + توكن الصفحة + ChatGPT-Account-ID
2. {account_id} + توكن الملف          ← مستقل عن الصفحة
3. نفس الأول بعد تحميل كامل            ← لما الصفحة تموت
التحقق: قراءتين will_renew=false بفاصل 2.5 ثانية (قراءة واحدة كذبت قبل كده)
```

---

## 6. حذف كارت أحمد (removeOurCard في auto_pay)

```
بيطابق آخر 4 أرقام من cards.json (فمبيمسّش كروت العميل)
   ├─ كارت أحمد مش على الحساب  → مفيش حاجة
   ├─ موجودة، مش الافتراضية    → احذف
   ├─ موجودة، الافتراضية + فيه غيرها → بدّل الافتراضية لكارت العميل، احذف
   └─ موجودة، وهي الوحيدة      → سيبها (الإلغاء يكفي)
التحقق: الكارت اختفت من قراءتين
علم --keep-cards لتعطيلها
```

**قرار أحمد المؤكد:** لو فيه كارت تانية (بتاعت العميل) → خليها الافتراضية
واحذف بتاعت أحمد. لو مفيش → اكتفِ بإلغاء التجديد.

---

## 7. الضريبة (3 محاولات متتالية)

```
املا العنوان الأمريكي → استنى الضريبة (لحد 30 ثانية)
   ├─ صفر → ادفع
   └─ لسه فيه ضريبة (VAT بدل Sales Tax صفر)
        ↓ محاولة 1: اختار اليمن (معفاة) → استنى → ارجع أمريكا → أعد ملء العنوان
        ├─ صفر → ادفع
        └─ لسه
             ↓ محاولة 2: أعد تحميل الصفحة كاملة → املا كارت+عنوان من الأول
             ├─ صفر → ادفع
             └─ لسه → قف
```

`waitForStableTax`: انتظار أدنى 30 ثانية، بيخرج بدري لو الضريبة اتغيرت
واستقرت 4 ثواني. `stillTaxed()` بيمنع محاولات اليمن/التحميل لو الضريبة صفر
أصلاً — فالحسابات العادية مش بتتأخر.

**ملاحظة:** أحمد جرّب تسلسل اليمن يدوياً ونجح. الحساب اللي شفنا فيه VAT 14%
كان مربوط بمصر (`billing_currency` مختلف).

---

## 8. الأخطاء اللي اتحلت — دروس مهمة

| # | الغلطة | الحل |
|---|--------|------|
| 1 | نجاح اتقرا "فشل" ("Think" = زرار شات) | confirm API يقرر، النص يتقرا بس على /checkout/ |
| 2 | مسار الفيز غلط | `/payments/payment_methods` مش `/payment_methods` |
| 3 | `--headless` يترفض دخوله | دايماً Xvfb + متصفح ظاهر |
| 4 | الإلغاء 422 | `{account_id}` في الجسم إجباري |
| 5 | الاشتراك مش بيظهر فوراً | استنى قبل الإلغاء (404 لثواني طبيعي) |
| 6 | مسح الكاش يسجّل خروج | اتشال نهائياً |
| 7 | التحقق بقراءة واحدة كذب | قراءتين متطابقتين بفاصل |
| 8 | الفحص في مرحلة الدفع (متأخر) | اتنقل لمرحلة الرابط |
| 9 | خطة بلا تواريخ = "مشترك" غلط | لازم active_until موجود ومستقبلي |
| 10 | كاشف الأسعار يقيس الشريط الجانبي | يبدأ من زرار الخطة (modal فوق التطبيق) |
| 11 | دوسة على خطة تانية | لازم اسم الخطة في الكارت، الحد 200 |
| 12 | التوكن يبطل بعد الدفع أحياناً | 3 طبقات بمصادر توكن مختلفة |
| 13 | "token missing" | احتياطي من الملف (global.__accessToken) |
| 14 | الافتراضية مبتتحذفش | بدّلها لكارت تاني الأول |
| 15 | التشخيص نفسه يكسر التشغيل | أي مراقبة لازم تكون في try/catch معزول |

**دروس عامة (كل واحد كلّف وقت):**
1. **متخمّنش — سجّل.** كل إصلاح جه من تسجيل API. كل تخمين ضيّع جولة.
2. **`node --check` مش كفاية** — بيفحص التركيب بس. استخدم `check_refs.js`
   للأسماء غير المعرّفة.
3. **CDP مبيلقطش أجسام الطلبات دايماً** — `body:none` في التسجيل مش معناه
   مفيش جسم. ضيّع جولتين على مسألة body الإلغاء والتجديد. الحل: اعتراض
   `window.fetch` + `XMLHttpRequest` من جوه الصفحة (زي record_all_api).
4. **العطل المتقطع مش دائم** — الإلغاء نجح مرتين وفشل مرتين بنفس الكود.
5. **الفشل الصامت أخطر من الصريح** — أي تخطي لازم يطبع سبب.
6. **الحالة الحقيقية > كود الحالة** — 200 مش دليل، اقرا الحالة بعده.

---

## 9. الأعلام (auto_pay.js)

```
--link-proxy=impulse   بروكسي مرحلة الرابط
--proxy=cheap          بروكسي الدفع
--country=PH           دولة الخروج
--watch=5              سكرين شوت كل 5 ثواني
--trace                كل ضغطة مفتاح + قيمة الحقل
--dry-run              املا وقف قبل Subscribe
--card=2               كارت معيّنة بالاسم أو الرقم
--clear-cards          امسح الفيز المحفوظة قبل الدفع
--keep-cards           سيب كارت أحمد بعد الدفع (يعطّل الحذف)
--cancel-existing      الغِ تجديد خطة قائمة
--keep-renewal         سيب التجديد شغّال
--allow-tax            ادفع حتى مع ضريبة
--reuse-link           استخدم آخر رابط في results/
```

---

## 10. الأدوات (كلها على جهاز أحمد المحلي للتجربة)

| الملف | الغرض | الحالة |
|-------|-------|--------|
| `auto_pay.js` | **الرئيسي** — المسار كامل | على السيرفر، موثوق |
| `cancel_lab3.js` | مصفوفة 11 طريقة إلغاء + اكتشاف | ثبّت الطرق |
| `card_lab.js` | حذف/تبديل الكروت | ثبّت المنطق |
| `record_all_api.js` | تسجيل كل API من جوه الصفحة | أداة التشخيص الأساسية |
| `check_refs.js` | كشف الأسماء غير المعرّفة | لازم بعد كل تعديل |

**مجلدات الناتج على السيرفر:** `payments/`, `results/`

**تقنية الاكتشاف الذهبية:** لما CDP يفشل في التقاط جسم طلب، اعترض
`window.fetch` و`XMLHttpRequest` من جوه الصفحة عبر
`Page.addScriptToEvaluateOnNewDocument`. ده بيشوف الأجسام الحقيقية.

---

## 11. حقائق البيئة

- **شبكة أحمد (TE Data)** بتقطع CONNECT على المنافذ غير القياسية —
  impulse (823) مش شغال من جهازه، شغال من السيرفر. Proxy-Cheap (8080) شغال
  من الاتنين.
- **Proxy-Cheap** بيرفض بـ 500 كتير (~50-80 إعادة محاولة/تشغيلة). المنطق
  بيعيد على 5xx/429، يستسلم على 403/407.
- **الكوكي > 4096 بايت** (حسابات Google) → حقن في الترويسات بدل التخزين.
- **الاشتراك بيبطّل accessToken** أحياناً (مش دايماً) — سبب الطبقات المتعددة.
- **backup_payment_method_enabled** غالباً true — لو الكارت الأساسية فشلت،
  ChatGPT بيجرب باقي الكروت. سبب وجود --clear-cards (قبل الدفع).

---

## 12. اللي فاضل / الخطوات الجاية

### أولوية قريبة
1. **تشغيلة تأكيد نظيفة** — بعد دمج الضريبة (3 محاولات) وحذف الكارت. تحقق:
   الضريبة نزلت من أول محاولة ولا احتاجت اليمن؟ سطر `Our card` في التقرير؟
2. **السرعة** — 230 ثانية، أغلبها بروكسي (65 إعادة محاولة). خيارات:
   بروكسي أسرع · إعادة استخدام المتصفح · **تشغيل متوازي** (3 متصفحات/سيرفر).

### المشروع الكبير الجاي: موقع التفعيل
الوصف الكامل والنموذج الحي اتعملوا (ملفات: `activation-site-spec.md`،
`activation_mockup.html`). الفكرة:
- العميل يشتري كود من **بوت خارجي**
- يدخل الموقع، يحط الكود + السيشن، يدوس تفعيل
- **مؤشر أخضر** يتقدم مع كل خطوة (خطوات مبسّطة مترجمة، مفيش تفاصيل تقنية)
- السيرفرات مشغولة؟ → طابور برقمه، يدخل تلقائي
- الكود **مرة واحدة**، يتحرق بعد النجاح فقط (مش الفشل)
- **4 سيرفرات × 3 متصفحات = 12 عملية متوازية**، توزيع حِمل تلقائي
- ChatGPT الآن، الباقي "قريباً"، دعم لغات كامل (عربي/إنجليزي/صيني/روسي)
- لوحة تحكم: أكواد، عمليات حية، سيرفرات، سجل بالدقيقة

**أسئلة مفتوحة للموقع:**
1. البوت يبعت الأكواد للموقع تلقائي (API) ولا أحمد يلصقها يدوي؟
2. العميل ساب الصفحة وهو في الطابور — إشعار ولا لازم يفضل فاتح؟
3. نحتفظ بالسكرين شوتس لكل عملية؟
4. الموقع يتحط فين (منفصل عن سيرفرات التنفيذ)؟

---

## 13. إزاي تبدأ المحادثة الجديدة

**أحمد يقول:**
> عندي مشروع أتمتة اشتراكات ChatGPT شغّال على سيرفر. مرفق وثيقة التسليم
> الكاملة و`auto_pay.js` — اقراهم وكمّل من "اللي فاضل".

**أحمد يرفق:** الوثيقة دي + آخر `auto_pay.js` من السيرفر:
```
scp root@72.60.184.150:~/chatgpt/auto_pay.js .
```

**المساعد:** يقرا، يفهم، يكمّل مباشرة. مفيش إعادة شرح. أسلوب التعديل: أوامر
`cd ~/chatgpt && cp auto_pay.js auto_pay.bak_X && python3 - << 'PYEOF'` مع
`print('patched' if old in s else 'NOT FOUND')` والتحقق بـ `node --check`
بعد كل تعديل. لو استبدال دالة كاملة: امشِ على الأقواس بعد **قوس المعاملات**
(بسبب `opts = {}`).

**الخطوة الأولى المقترحة:** تشغيلة تأكيد نظيفة على حساب free جديد، للتحقق من
كل الطبقات مع بعض (ضريبة + إلغاء + حذف كارت).

---

*آخر تحديث: المسار كامل بكل الطبقات. الإلغاء وحذف الكارت والضريبة كلهم مبنيين
على قياس فعلي مسجّل. باقي: تشغيلة تأكيد، ثم السرعة/التوازي، ثم الموقع.*
